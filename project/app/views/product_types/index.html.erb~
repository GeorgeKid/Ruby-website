<p id="notice"><%= notice %></p>


<table class="table">
  <thead>
    <tr>
      <th>产品类型</th>
      <th>产品</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @product_types.each do |product_type| %>
      <tr>
        <td><%= product_type.product_type_name %></td>
        <td><%= (product_type.products.collect {link_to |product|  product.product_name  }).join(", ") %></td>
        <td><%= link_to '显示', product_type %></td>
        <td><%= link_to '编辑', edit_product_type_path(product_type) %></td>
        <td><%= link_to '删除', product_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '创建新的产品类型', new_product_type_path %>
<br>

